# 颜值矩阵分析系统 — 依赖
# 后端依赖 (Flask API)

flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.24.0
opencv-python>=4.8.0
mediapipe>=0.10.0
pillow>=10.0.0
pyyaml>=6.0
scipy>=1.11.0
rembg>=2.0.50
onnxruntime>=1.16.0

scikit-learn>=1.3.0

# 桌面GUI依赖
customtkinter>=5.2.0

# 打包工具 (可选)
# pyinstaller>=6.0.0

# Web 服务部署 (可选)
# gunicorn>=21.2.0
# waitress>=2.1.0
